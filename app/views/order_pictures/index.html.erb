<h1>Listing order_pictures</h1>

<table>
  <tr>
    <th>Quantity</th>
    <th>Price</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @order_pictures.each do |order_picture| %>
  <tr>
    <td><%= order_picture.quantity %></td>
    <td><%= order_picture.price %></td>
    <td><%= link_to 'Show', order_picture %></td>
    <td><%= link_to 'Edit', edit_order_picture_path(order_picture) %></td>
    <td><%= link_to 'Destroy', order_picture, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Order picture', new_order_picture_path %>
