<h1>Dash</h1>
<%= link_to "Picture", pictures_path(@picture) %>
<%= link_to "Categories", categories_path(@category) %>
<%= link_to "Artists", artists_path(@artist) %>
<h1>Listing pictures</h1>

<table>
  <tr>
    <th>Picture name</th>
    <th>Price</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pictures.each do |picture| %>
  <tr>
    <td><%= picture.picture_name %></td>
    <td><%= picture.price %></td>
    <td><%= picture.description %></td>
    <td><%= link_to 'Show', picture %></td>
    <td><%= link_to 'Edit', edit_picture_path(picture) %></td>
    <td><%= link_to 'Destroy', picture, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Picture', new_picture_path %>
